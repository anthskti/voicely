# Audio fixtures (Phase 0)

Synthetic mono 16 kHz WAVs for Bruno / local API tests (not real VO takes).

| File | Role |
|------|------|
| `user_take_good_00.wav` … `_02.wav` | Near reference length/level |
| `user_take_short_00.wav` | Too short (~0.6s) |
| `user_take_quiet_00.wav` | Low amplitude |

Regenerate with any tone generator if needed; keep filenames so Bruno requests stay valid.