# Sample scenes (Phase 0)

Two placeholder scenes for contracts and Bruno. Swap transcripts/timestamps for your real lines when ready; keep the same JSON shape.

| Scene | Chunks | Refs |
|-------|--------|------|
| `scene_01/` | 3 lines (morning brief copy) | `refs/chunk_0{0,1,2}.wav` (generated tones) |
| `scene_02/` | 3 lines (product demo copy) | same |

`video_no_vox.mp4` is not checked in yet — add stem videos before Phase 4 UI work. URLs use `example.invalid` until S3 (Phase 2).

Local paths in `chunks.json` (`*_path`) are for tooling; Base44 / API use `*_url`.