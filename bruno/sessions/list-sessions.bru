meta {
  name: List sessions
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/api/v1/sessions?user_id={{userId}}&scene_id={{sceneId}}
  body: none
  auth: none
}

headers {
  Accept: application/json
}

docs {
  Optional filters: user_id, scene_id. Newest first.
}
